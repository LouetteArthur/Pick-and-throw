
verbose=1, buffer_size = 100, batch_size = 100, learning_rate = 0.0001, tau = 0.005, train_freq = 1, gradient_steps = 1
action_noise = NormalActionNoise(mean=np.zeros(env.observation_space.shape[0]), sigma=0.05 * np.ones(env.observation_space.shape[0]))